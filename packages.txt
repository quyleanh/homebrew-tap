# packages.txt — top-level packages only (your brew leaves)
# Edit this file to add/remove packages.
# Dependencies are resolved automatically by build.sh at CI runtime.

# CLI / download tools
argon2
aria2
wget
fzf
jq
tree
yt-dlp
rclone

# Shell
bash
tmux

# Media
ffmpeg
libx11
scrcpy

# Dev / runtimes
little-cms2
node
# node's published bottle links these two, but homebrew/core dropped them from the
# node formula's dependency list (2026-09-19), so `brew deps` no longer resolves them
# and the pipeline would silently never refresh them again. Keep them as explicit
# leaves while a published node still links them; drop them once a node rebuild
# produces a bottle that no longer does.
libnghttp3
libngtcp2
hugo
rtk
go
docker
docker-compose
colima
nasm
git-filter-repo
gitleaks
potrace
gh
